<project name="Patch" basedir=".">

	<target name="Patch">
		<exec executable="cmd" dir="${homePath}" outputproperty="test">
			<arg value="/c" />
			<arg value="${cmd}" />
		</exec>

	</target>
</project>
